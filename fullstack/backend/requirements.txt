# OSINT-CI-TOOL Backend — Python Dependencies
# Pinned for reproducibility as of July 30, 2026.
# Upgrade cadence: patch versions freely, minor with testing.

# ── Web Framework ──────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ── HTTP Client (async, used to call Anthropic API) ────────────
httpx==0.27.2

# ── Data Validation ────────────────────────────────────────────
pydantic==2.10.1

# ── Environment Variable Loading ───────────────────────────────
python-dotenv==1.0.1
